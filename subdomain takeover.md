
A hostile subdomain happens when:

A company creates a subdomain

The subdomain points to an external service (like GitHub Pages, AWS, Azure, etc.)

The service is deleted / expired / not properly removed

An attacker registers that resource → and takes control of the subdomain

 This is also called Subdomain Takeover.

 


